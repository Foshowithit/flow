{
  "name": "flow-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "validate:gateway": "npm --prefix apps/gateway run validate",
    "compose-config:gateway": "npm --prefix apps/gateway run compose-config",
    "validate:desktop": "npm --prefix apps/desktop run typecheck && npm --prefix apps/desktop run lint && npm --prefix apps/desktop run build",
    "build:desktop": "npm --prefix apps/desktop run build",
    "smoke:desktop": "MOCK_CHAT=true npm --prefix apps/desktop run smoke",
    "validate": "npm run validate:gateway && npm run validate:desktop"
  }
}
